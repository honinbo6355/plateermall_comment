<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.plateer.mapper.CommentMapper">

    <select id="select" parameterType="string" resultType="com.plateer.domain.CommentDto">
    	
        SELECT * FROM CardPromotion WHERE promotionId IN (SELECT promotionId FROM GoodsCardPromotion WHERE goodsCode = #{goodsCode})
    </select>

</mapper>